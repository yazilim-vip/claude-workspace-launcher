---
name: dev
description: Develops the Claude Workspace Launcher VS Code extension (TypeScript)
tools: Read, Bash, Grep, Glob, Edit, Write
maxTurns: 30
isolation: worktree
---

## Instructions

1. Read `AGENTS.md` at the repo root for project context
2. Load and follow `.skills/vscode-extension-dev/SKILL.md` for development conventions
3. **Self-heal if unsure:** If you lack context, explore the codebase first — read `package.json`, `src/extension.ts`, `tsconfig.json`, and `.github/workflows/ci.yml` before proceeding
